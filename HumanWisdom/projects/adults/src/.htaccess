# <IfModule mod_rewrite.c>

# RewriteEngine On
# # Serve existing files and directories directly
# RewriteCond %{REQUEST_FILENAME} -f [OR]
# RewriteCond %{REQUEST_FILENAME} -d
# RewriteRule ^ - [L]

# # Redirect all requests within the "course" project to index.html
# RewriteRule ^(.*)$ /course/index.html [NC,L]
# </IfModule>
