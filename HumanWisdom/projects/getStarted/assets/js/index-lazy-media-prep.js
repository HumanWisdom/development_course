/**
 * Chunk HTML is prepared by prepareIndexLazySection in index.js after insert.
 * A document-wide MutationObserver here caused extra main-thread work and
 * disconnected before chunks loaded, so it is intentionally omitted.
 */
(function () {
    "use strict";
})();
